import{useMemo,useState}from'react';

export default function TraceTeamFocused({people=[],departments=[],projects=[],onInvite,onAddDepartment,onAssign}){
 const[tab,setTab]=useState('people'),[inviteOpen,setInviteOpen]=useState(false),[depOpen,setDepOpen]=useState(false),[assign,setAssign]=useState(null),[email,setEmail]=useState(''),[role,setRole]=useState('operator'),[depName,setDepName]=useState(''),[projectId,setProjectId]=useState(''),[projectRole,setProjectRole]=useState('member');
 const empty=!people.length&&!departments.length;
 const activeProjects=useMemo(()=>projects.filter(p=>p.status!=='completed'),[projects]);
 async function invite(e){e.preventDefault();if(!email.trim())return;await onInvite({email:email.trim(),role});setEmail('');setInviteOpen(false)}
 async function addDepartment(e){e.preventDefault();if(!depName.trim())return;await onAddDepartment({name:depName.trim()});setDepName('');setDepOpen(false)}
 async function assignProject(e){e.preventDefault();if(!assign||!projectId)return;await onAssign(projectId,assign.type==='person'?{userId:assign.id,projectRole}:{departmentId:assign.id,projectRole});setAssign(null);setProjectId('');setProjectRole('member')}
 if(empty)return <div className="grid min-h-[70vh] place-items-center"><div className="max-w-md text-center"><div className="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-blue-50 text-3xl text-blue-600">♙</div><h1 className="mt-5 text-3xl font-black">Agrega tu equipo</h1><p className="mt-2 text-sm leading-6 text-slate-500">Invita personas o crea departamentos. TRACE mostrará después solo su función y los proyectos donde participan.</p><div className="mt-6 flex justify-center gap-2"><button onClick={()=>setInviteOpen(true)} className="rounded-xl bg-blue-600 px-5 py-3 text-sm font-black text-white">+ Agregar persona</button><button onClick={()=>setDepOpen(true)} className="rounded-xl border border-slate-200 bg-white px-5 py-3 text-sm font-black">+ Departamento</button></div></div>{inviteOpen&&<Modal title="Agregar persona" onClose={()=>setInviteOpen(false)}><form onSubmit={invite}><input autoFocus value={email} onChange={e=>setEmail(e.target.value)} className="w-full rounded-xl border px-4 py-3 text-sm" placeholder="correo@empresa.com"/><select value={role} onChange={e=>setRole(e.target.value)} className="mt-3 w-full rounded-xl border px-4 py-3 text-sm"><option value="operator">Operador</option><option value="manager">Supervisor</option><option value="viewer">Consulta</option></select><Submit>Invitar</Submit></form></Modal>}{depOpen&&<Modal title="Nuevo departamento" onClose={()=>setDepOpen(false)}><form onSubmit={addDepartment}><input autoFocus value={depName} onChange={e=>setDepName(e.target.value)} className="w-full rounded-xl border px-4 py-3 text-sm" placeholder="Ej. Terminaciones"/><Submit>Agregar</Submit></form></Modal>}</div>;
 return <div className="mx-auto max-w-[1500px] space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-3xl font-black">Equipo</h1><p className="mt-1 text-sm text-slate-500">Quién participa, qué función tiene y dónde está asignado.</p></div><div className="flex gap-2"><button onClick={()=>setDepOpen(true)} className="rounded-xl border bg-white px-4 py-2.5 text-xs font-black">+ Departamento</button><button onClick={()=>setInviteOpen(true)} className="rounded-xl bg-blue-600 px-4 py-2.5 text-xs font-black text-white">+ Persona</button></div></div><div className="inline-flex rounded-xl border border-slate-200 bg-white p-1"><button onClick={()=>setTab('people')} className={`rounded-lg px-4 py-2 text-xs font-black ${tab==='people'?'bg-slate-950 text-white':'text-slate-500'}`}>Personas · {people.length}</button><button onClick={()=>setTab('departments')} className={`rounded-lg px-4 py-2 text-xs font-black ${tab==='departments'?'bg-slate-950 text-white':'text-slate-500'}`}>Departamentos · {departments.length}</button></div>{tab==='people'?<div className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="grid grid-cols-[1.2fr_.55fr_.8fr_1.2fr_auto] gap-3 border-b bg-slate-50 px-5 py-3 text-[10px] font-black uppercase tracking-wide text-slate-400"><span>Persona</span><span>Función</span><span>Departamento</span><span>Proyectos</span><span/></div>{people.map(p=><div key={p.id} className="grid grid-cols-[1.2fr_.55fr_.8fr_1.2fr_auto] items-center gap-3 border-b border-slate-100 px-5 py-4 last:border-0"><div className="flex min-w-0 items-center gap-3"><span className="grid h-9 w-9 shrink-0 place-items-center rounded-full bg-slate-900 text-[10px] font-black text-white">{p.email.slice(0,2).toUpperCase()}</span><span className="truncate text-xs font-black">{p.email}</span></div><span className="text-xs text-slate-600">{labelRole(p.role)}</span><span className="text-xs text-slate-500">{p.departments?.join(', ')||'—'}</span><span className="text-xs text-slate-500">{p.projects?.join(', ')||'Sin proyecto'}</span><button onClick={()=>setAssign({type:'person',id:p.id,label:p.email})} className="rounded-lg border px-3 py-2 text-[10px] font-black">Asignar</button></div>)}</div>:<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{departments.map(d=><div key={d.id} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex items-start justify-between"><div><h2 className="font-black">{d.name}</h2><p className="mt-1 text-xs text-slate-500">{d.member_count||0} miembros</p></div><button onClick={()=>setAssign({type:'department',id:d.id,label:d.name})} className="rounded-lg border px-3 py-2 text-[10px] font-black">Asignar</button></div><div className="mt-5 text-[10px] font-black uppercase tracking-wide text-slate-400">Proyectos</div><div className="mt-2 text-xs text-slate-600">{d.projects?.join(', ')||'Sin proyecto asignado'}</div></div>)}</div>}{inviteOpen&&<Modal title="Agregar persona" onClose={()=>setInviteOpen(false)}><form onSubmit={invite}><input autoFocus value={email} onChange={e=>setEmail(e.target.value)} className="w-full rounded-xl border px-4 py-3 text-sm" placeholder="correo@empresa.com"/><select value={role} onChange={e=>setRole(e.target.value)} className="mt-3 w-full rounded-xl border px-4 py-3 text-sm"><option value="operator">Operador</option><option value="manager">Supervisor</option><option value="viewer">Consulta</option></select><Submit>Invitar</Submit></form></Modal>}{depOpen&&<Modal title="Nuevo departamento" onClose={()=>setDepOpen(false)}><form onSubmit={addDepartment}><input autoFocus value={depName} onChange={e=>setDepName(e.target.value)} className="w-full rounded-xl border px-4 py-3 text-sm" placeholder="Nombre del departamento"/><Submit>Agregar</Submit></form></Modal>}{assign&&<Modal title={`Asignar · ${assign.label}`} onClose={()=>setAssign(null)}><form onSubmit={assignProject}><p className="text-xs text-slate-500">Selecciona únicamente el proyecto y la función dentro de ese proyecto.</p><select value={projectId} onChange={e=>setProjectId(e.target.value)} className="mt-4 w-full rounded-xl border px-4 py-3 text-sm"><option value="">Proyecto</option>{activeProjects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><select value={projectRole} onChange={e=>setProjectRole(e.target.value)} className="mt-3 w-full rounded-xl border px-4 py-3 text-sm"><option value="member">Miembro</option><option value="supervisor">Supervisor</option><option value="manager">Responsable</option><option value="observer">Observador</option></select><Submit>Asignar al proyecto</Submit></form></Modal>}</div>;
}
function Modal({title,onClose,children}){return <div className="fixed inset-0 z-[120] grid place-items-center bg-slate-950/40 p-4"><div className="w-full max-w-md rounded-3xl bg-white p-6 shadow-2xl"><div className="mb-5 flex items-center justify-between"><h2 className="text-xl font-black">{title}</h2><button onClick={onClose} className="text-xl text-slate-400">×</button></div>{children}</div></div>}
function Submit({children}){return <button className="mt-5 w-full rounded-xl bg-blue-600 px-4 py-3 text-sm font-black text-white">{children}</button>}
function labelRole(v){return({operator:'Operador',manager:'Supervisor',viewer:'Consulta',tenant:'Administrador',superadmin:'Administrador'})[v]||v||'Miembro'}
