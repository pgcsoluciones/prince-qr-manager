function sourceLabel(v){return({system:"Sistema",operator:"Operativo",supervisor:"Supervisor",admin:"Panel administrativo",public:"Enlace público",integration:"Integración",dashboard:"Panel",mobile:"Móvil",qr:"QR",offline_sync:"Sincronización offline"})[v]||String(v||"Sistema")}
function pretty(v){return String(v||"").replaceAll("_"," ")}
export default function TraceTimelineEventDrawer({event,onClose,onContextAction}){
 if(!event)return null;
 const p=event.payload||{},previous=p.previous_status||p.previousStatus||p.from_status||p.fromStatus||null,next=p.new_status||p.newStatus||p.to_status||p.toStatus||event.activity_status||null;
 return <div className="fixed inset-0 z-[80] flex justify-end bg-slate-950/30" onMouseDown={e=>e.target===e.currentTarget&&onClose?.()}><aside className="h-full w-full max-w-[560px] overflow-y-auto bg-white shadow-2xl"><div className="sticky top-0 z-10 flex items-start justify-between border-b border-slate-200 bg-white px-5 py-5"><div><div className="text-xs font-black uppercase tracking-[.16em] text-blue-600">Registro protegido</div><h2 className="mt-1 text-2xl font-black">Detalle del evento</h2></div><button onClick={onClose} className="grid h-10 w-10 place-items-center rounded-xl border border-slate-200 text-xl">×</button></div><div className="space-y-5 p-5"><section className="rounded-2xl border border-slate-200 p-5"><div className="text-sm font-black">{event.description||pretty(event.event_type)}</div><div className="mt-3 grid gap-3 sm:grid-cols-2"><Info label="Fecha y hora" value={event.occurred_at?new Date(event.occurred_at).toLocaleString("es-DO"):"—"}/><Info label="Autor" value={event.actor_email||event.actor_role||"Trace"}/><Info label="Etapa" value={event.stage_name||"Proyecto"}/><Info label="Actividad" value={event.activity_title||event.execution_code||"—"}/><Info label="Origen" value={sourceLabel(event.event_source)}/><Info label="Identificador" value={event.id}/></div></section>{(previous||next)&&<section className="rounded-2xl border border-slate-200 p-5"><h3 className="font-black">Cambio de estado</h3><div className="mt-4 flex items-center gap-3"><span className="rounded-lg bg-slate-100 px-3 py-2 text-xs font-bold">{previous?pretty(previous):"Estado previo no registrado"}</span><span className="text-slate-400">→</span><span className="rounded-lg bg-blue-50 px-3 py-2 text-xs font-black text-blue-700">{next?pretty(next):"Sin cambio"}</span></div></section>}{(event.evidenceUrls?.length||event.evidenceNames?.length||Number(event.evidence_count)>0)&&<section className="rounded-2xl border border-slate-200 p-5"><h3 className="font-black">Evidencias</h3><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-3">{(event.evidenceUrls||[]).map((u,i)=><a key={u+i} href={u} target="_blank" rel="noreferrer" className="overflow-hidden rounded-xl border border-slate-200"><img src={u} alt="Evidencia" className="h-28 w-full object-cover"/><div className="truncate px-2 py-2 text-[10px] text-slate-500">{event.evidenceNames?.[i]||`Evidencia ${i+1}`}</div></a>)}{!event.evidenceUrls?.length&&(event.evidenceNames||[]).map(n=><div key={n} className="rounded-xl border border-slate-200 p-3 text-xs">▤ {n}</div>)}</div></section>}<section className="rounded-2xl border border-blue-100 bg-blue-50 p-5"><div className="flex items-start gap-3"><span>🔒</span><div><h3 className="text-sm font-black text-blue-950">Este registro no se edita</h3><p className="mt-1 text-xs leading-5 text-blue-900/70">Si existe un error, Trace debe registrar una corrección o invalidación como un evento nuevo. El registro original permanece en la bitácora.</p></div></div></section><div className="flex flex-wrap gap-3"><button onClick={()=>onContextAction?.("comment",event)} className="rounded-xl border border-slate-300 px-4 py-3 text-xs font-black">Añadir comentario</button><button onClick={()=>onContextAction?.("evidence",event)} className="rounded-xl border border-blue-500 px-4 py-3 text-xs font-black text-blue-600">Añadir evidencia</button>{String(event.activity_status)==="correction_required"&&<button onClick={()=>onContextAction?.("correction",event)} className="rounded-xl bg-orange-500 px-4 py-3 text-xs font-black text-white">Responder corrección</button>}</div></div></aside></div>
}
function Info({label,value}){return <div><div className="text-[10px] font-black uppercase tracking-[.12em] text-slate-400">{label}</div><div className="mt-1 break-words text-sm font-semibold text-slate-700">{value||"—"}</div></div>}
